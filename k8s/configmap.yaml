apiVersion: v1
kind: ConfigMap
metadata:
  name: ecommerce-config
  namespace: default
  labels:
    app: devops-ecommerce
data:
  APP_ENV: "production"
  MOCK_BACKEND_ENABLED: "true"
  K8S_CLUSTER_NODE: "aws-eks-production"
  PROMETHEUS_METRICS_PATH: "/metrics"
